/* Statically types - Must write the data type before using it, 
    memory allocated during compile time, but are faster

    variable shouldn't begin with number
*/
public class HelloWorld {
    public static void main(String []args) {
        int age = 10;
        String name = "Nithin";
        System.out.println(age + name + a);
    }
}